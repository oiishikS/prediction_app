# Fun Prediction App Monorepo

Lightweight MVP monorepo for a social prediction game app.

## Structure

- `frontend/` - Next.js + React + Tailwind frontend (mobile-first)
- `backend/` - backend/API service (planned, not implemented yet)

## Frontend quick start

```bash
cd frontend
npm install
npm run dev
```

Then open `http://localhost:3000`.
